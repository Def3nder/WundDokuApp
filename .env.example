# Datenbank (SQLite, lokal)
DATABASE_URL="file:./dev.db"

# Auth.js - mit `openssl rand -base64 32` erzeugen
AUTH_SECRET="bitte-ersetzen"
AUTH_TRUST_HOST=true

# Ablageort fuer Wundfotos (ausserhalb von public/)
STORAGE_DIR="./storage"

# Zugangsdaten des ersten Admin-Kontos (nur fuer den Seed)
SEED_ADMIN_EMAIL="admin@praxis.local"
SEED_ADMIN_PASSWORD="WundDoku!2026"
